<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>UTB Minute Canteen</title>
    <base href="/" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
</head>
<body>
    <Routes />
    <script src="_framework/blazor.server.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

@using Microsoft.AspNetCore.Authentication
@inject UTB.Minute.Web.Services.TokenProvider TokenProvider

@code {
    [CascadingParameter]
    public HttpContext HttpContext { get; set; } = default!;

    protected override async Task OnInitializedAsync()
    {
        if (HttpContext != null)
        {
            var token = await HttpContext.GetTokenAsync("access_token");
            System.Console.WriteLine($"[DEBUG App.razor] AccessToken retrieved: {(!string.IsNullOrEmpty(token))} (Length: {token?.Length ?? 0})");
            TokenProvider.AccessToken = token;
        }
    }
}